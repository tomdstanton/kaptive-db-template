name: Release Database

on:
  push:
    branches:
      - main

jobs:
  call-central-workflow:
    # Point this directly to your central repository and the specific workflow file
    uses: klebgenomics/Kaptive/.github/workflows/database_cicd.yml@main
    permissions:
      contents: write # Pass write permissions down to the central workflow
